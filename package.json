{
  "name": "erp-universitario",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo oficial para el ERP Universitario",
  "scripts": {
    "predev": "pnpm --filter @erp/contracts build",
    "dev": "pnpm --filter \"@erp/*\" --parallel dev",
    "build": "pnpm --filter \"@erp/*\" build"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
